# Dependencias SOLO para scripts/reindex.py -- correr localmente,
# no van dentro del Dockerfile de la API (mantiene la imagen liviana).
boto3==1.35.0
chromadb==0.5.20
pyyaml==6.0.2
langchain-core==0.3.15
langchain-text-splitters==0.3.2
